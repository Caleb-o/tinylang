# This is the same as "say_hello(name: string, age: int): void"
fn say_hello(name: string, age: int) {
	var int x = 10 + age;
	var int y = x * x;
}

fn bar(foo: boolean) {
	var boolean x = -foo;
}

var string n = "Caleb";
say_hello(n, 22);

@println("Hello!", " Foo ", 123);