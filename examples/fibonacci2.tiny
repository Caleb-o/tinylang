# Fibonacci using variable swaps instead of recursion
let fib = function(nth: int) {
	var a = 0, b = 1, c = 0;

	if (nth == 0) {
		print(0);
		return;
	}

	while var i = 2; i <= nth {
		i = i + 1;

		c = a + b;
		a = b;
		b = c;
	}
	
	print(b);
};

fib(32); # 2178309