var int a = 10;

# do_things has an explicit return value of 20
fn do_things(b: var int): int(20) {
	b = b * b;

	fn do_other_things(b: var int) {
		b = b * 2;
	};

	do_other_things(b);
	result = b;
}

fn check(): int(100) {}

let int bar = check();
@println(bar);

fn try_pass_immutable_to_mutable(value: var int) {
	@println("This should not happen!");
}

try_pass_immutable_to_mutable(bar);

let int foo = do_things(a);
@println(a, " " + "Hello! ", foo);