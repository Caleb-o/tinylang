# FIXME: Fibonacci currently is broken. I assume it could be something to do with the new reference/type system

fn fibonacci(n: int): int {
	if n > 1 {
		result = fibonacci(n - 1) + fibonacci(n - 2);
	}
}

@println(fibonacci(24)); # 46368