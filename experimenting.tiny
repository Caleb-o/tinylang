var int a = 10;

# do_things has an explicit return value of 20
fn do_things(b: var int): int(20) {
	b = b * b;

	# Override the result
	# FIXME: The value of b here is the reference value of a, 
	# it should also have its own value in this scope
	result = b;
}

let int foo = do_things(a);
@println(a, " " + "Hello! ", foo);

# fn bar(): int {
# 	return 10;
# }

# let int foo = bar();