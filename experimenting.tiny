# Note: Not currently supported, but this is what using tuples could look like

fn return_multiple(): [boolean(true), int(10)] {
	# result = [true, 10]
}

# [int] - list
# [string:int] - dictionary
# [boolean, int] - tuple?

# Unpack a tuple
let [boolean ok, int value] = return_multiple();
@println(ok, ": value = ", value);

# Store a tuple
let [boolean, int] state = return_multiple();
@println(state[0], ": value = ", state[1]);